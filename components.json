{
  "//": "shadcn/ui project config. Lets you run `npx shadcn@latest add <component>` later to drop more primitives into components/ui. The primitives already in components/ui (card, badge, skeleton, alert) were generated to match this config.",
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "app/globals.css",
    "baseColor": "slate",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui"
  }
}
