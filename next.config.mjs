/**
 * Next.js configuration.
 * Kept intentionally minimal for Phase 1. The App Router is on by default in
 * Next 14 (no experimental flag needed).
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
